// 已完成
当前node 状态正常的前提条件时：
  所有连向当前node的所有node的状态时正常的
  当前node的配置已配置好。

// 已完成
配置当前节点的前提是: 前面连接的node都配置好了

// 已完成
连线状态: 两端的node配置都是正常的。
  当配置好后,获取所有target为当前节点的线,改变其状态

完成每个node的配置drawer文件
  1. 完成filter节点的操作。